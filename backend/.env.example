# ===========================================
# DAYONME 환경 설정 예시
# ===========================================
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요

# 서버 설정
NODE_ENV=development
PORT=3001
HOST=localhost

# ===========================================
# 데이터베이스 설정
# ===========================================
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=dayonme
DB_PORT=3306
DB_DIALECT=mysql

# ===========================================
# 보안 설정 (필수 - 반드시 변경 필요)
# ===========================================
# JWT_SECRET 생성: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_strong_random_secret_key_here_minimum_32_characters
JWT_EXPIRATION=24h
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=7d
BCRYPT_ROUNDS=12

# CSRF 보안 설정 (프로덕션에서 true 권장)
CSRF_ENABLED=false

# 모바일 앱 API 키 (프로덕션에서 설정 권장)
# MOBILE_API_KEY=your_secure_api_key_here

# 엄격한 Origin 검사 (프로덕션에서 true 권장)
STRICT_ORIGIN_CHECK=false

# HTTPS 강제 리다이렉트 (프로덕션에서 true 설정)
HTTPS_ONLY=false

# ===========================================
# CORS 설정
# ===========================================
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081
CLIENT_URL=http://localhost:3000,http://localhost:8081

# ===========================================
# 관리자 계정 설정
# ===========================================
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_strong_admin_password
ADMIN_EMAILS=admin@example.com
RESET_ADMIN_PASSWORD=true

# ===========================================
# Redis 캐싱 설정 (성능 최적화 - 강력 권장!)
# ===========================================
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ===========================================
# Rate Limiting 설정
# ===========================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ===========================================
# 로깅 설정
# ===========================================
LOG_LEVEL=debug
LOG_FORMAT=dev

# ===========================================
# 파일 업로드 설정
# ===========================================
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# ===========================================
# 이메일 설정 (선택사항)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
EMAIL_FROM_NAME=Dayonme Support

# ===========================================
# 소셜 로그인 설정 (선택사항)
# ===========================================
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret
