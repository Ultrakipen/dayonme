# 🚀 프로덕션 환경 설정 예시
# 실제 배포 시 이 파일을 .env.production으로 복사하여 사용

# 서버 설정
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# 데이터베이스 (프로덕션)
DB_HOST=your-production-db-host
DB_PORT=3306
DB_USER=your-db-user
DB_PASSWORD=your-strong-db-password
DB_NAME=iexist_production

# JWT 시크릿 (최소 256bit, 주기적 변경 권장)
JWT_SECRET=CHANGE_THIS_TO_STRONG_RANDOM_STRING_256BIT_OR_MORE
JWT_REFRESH_SECRET=CHANGE_THIS_TO_DIFFERENT_STRONG_RANDOM_STRING

# Redis (캐싱)
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# 파일 업로드
UPLOAD_MAX_FILE_SIZE=5242880  # 5MB
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/webp
UPLOAD_PATH=/var/www/uploads

# 보안
BCRYPT_ROUNDS=12
CORS_ORIGIN=https://yourdomain.com
HTTPS_ONLY=true

# Rate Limiting (프로덕션 권장값)
RATE_LIMIT_READ_MAX=200
RATE_LIMIT_WRITE_MAX=15
RATE_LIMIT_AUTH_MAX=5
RATE_LIMIT_UPLOAD_MAX=5
RATE_LIMIT_INTERACTION_MAX=30
RATE_LIMIT_REPORT_MAX=3

# 데이터베이스 커넥션 풀
DB_POOL_MAX=50
DB_POOL_MIN=10
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# 로깅
LOG_LEVEL=info
LOG_FILE=./logs/production.log

# CDN (선택사항)
CDN_URL=https://cdn.yourdomain.com

# 관리자 계정 (초기 설정용, 설정 후 삭제 권장)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=CHANGE_THIS_IMMEDIATELY

# 외부 서비스 (필요 시)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=ap-northeast-2
S3_BUCKET=your-s3-bucket
